{
  "version": "0.1.1",
  "license": "MIT",
  "name": "@nestjs-toolkit/chunk-stream",
  "author": "Valmir Barbosa",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "module": "dist/chunk-stream.esm.js",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "start": "tsdx watch --targe node --format cjs",
    "build": "tsdx build --targe node --format cjs",
    "test": "tsdx test",
    "lint": "tsdx lint --fix",
    "prepare": "tsdx build",
    "publish:prod": "cd dist && npm publish --access public",
    "publish:local": "yarn publish --registry http://localhost:4873"
  },
  "peerDependencies": {},
  "husky": {
    "hooks": {
      "pre-commit": "tsdx lint"
    }
  },
  "prettier": {
    "printWidth": 80,
    "semi": true,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "tsdx": "^0.14.1",
    "tslib": "^2.5.2",
    "typescript": "^5.0.4"
  }
}
